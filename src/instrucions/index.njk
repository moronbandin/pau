---
layout: base.njk
title: Instrucións e ponderacións PAU
subject: default
description: Instrucións oficiais da PAU (xerais, Latín e Grego) e táboa de ponderacións — pendentes de publicación pola CIUG.
---
{% from "icons.njk" import icon %}
<div class="section-head">
  <div class="wrap">
    <p class="breadcrumb"><a href="/">Inicio</a> / Instrucións e ponderacións</p>
    <p class="eyebrow">CIUG</p>
    <h1>Instrucións e ponderacións</h1>
    <p>Espazo reservado para os documentos oficiais que a CIUG aínda non publicou para o vindeiro curso. En canto saian, súbense aquí tal cal, sen modificar.</p>
  </div>
</div>
<div class="section-body">
  <div class="wrap">
    <div class="pending-list">

      <div class="glass-card pending-card">
        <div class="pending-card-head">
          <span class="pending-icon">{{ icon("doc") }}</span>
          <div>
            <h2>Instrucións xerais da PAU</h2>
            <p class="pending-sub">Normativa e instrucións xerais para todas as materias.</p>
          </div>
          <span class="pending-badge">{{ icon("clock", "sub") }} Pendente</span>
        </div>
        <p class="pending-body">Aínda non publicadas pola CIUG. Cando saian, colócase aquí o PDF oficial completo.</p>
      </div>

      <div class="glass-card pending-card">
        <div class="pending-card-head">
          <span class="pending-icon" style="color:var(--latin-accent)">{{ icon("doc") }}</span>
          <div>
            <h2>Instrucións específicas — Latín</h2>
            <p class="pending-sub">Estrutura do exame, criterios de cualificación e materiais permitidos para Latín II.</p>
          </div>
          <span class="pending-badge">{{ icon("clock", "sub") }} Pendente</span>
        </div>
        <p class="pending-body">Aínda non publicadas. Substitúense por este aviso en canto a CIUG as saque.</p>
      </div>

      <div class="glass-card pending-card">
        <div class="pending-card-head">
          <span class="pending-icon" style="color:var(--greek-accent)">{{ icon("doc") }}</span>
          <div>
            <h2>Instrucións específicas — Grego</h2>
            <p class="pending-sub">Estrutura do exame, criterios de cualificación e materiais permitidos para Grego II.</p>
          </div>
          <span class="pending-badge">{{ icon("clock", "sub") }} Pendente</span>
        </div>
        <p class="pending-body">Aínda non publicadas. Substitúense por este aviso en canto a CIUG as saque.</p>
      </div>

      <div class="glass-card pending-card">
        <div class="pending-card-head">
          <span class="pending-icon">{{ icon("table") }}</span>
          <div>
            <h2>Táboa de ponderacións deste curso</h2>
            <p class="pending-sub">Canto ponderan Latín e Grego II para o acceso ás distintas titulacións universitarias. Publícase normalmente en abril.</p>
          </div>
          <span class="pending-badge">{{ icon("clock", "sub") }} Sae en abril</span>
        </div>
        <p class="pending-body">En canto se publique, engádese aquí a táboa completa (materia × titulación × universidade), consultable e non só en PDF.</p>
      </div>

    </div>

    <p class="pending-footnote">Fonte oficial cando estea dispoñible: <a href="https://ciug.gal" target="_blank" rel="noopener">ciug.gal</a>. Esta páxina non inventa datos: mentres a CIUG non publique cada documento, queda marcado coma "pendente".</p>
  </div>
</div>
